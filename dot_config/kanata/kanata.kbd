;; Keyboard-no-Jutsu: F16 -> tap Esc, hold Ctrl+Alt+Super
;; Dygma sends plain F16, kanata handles tap/hold

(defcfg
  process-unmapped-keys no
)

(defsrc
  f16
)

(defalias
  hyper (tap-hold 200 200 esc (multi lctl lalt lmet))
)

(deflayer default
  @hyper
)
